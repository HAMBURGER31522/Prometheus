[package]
name = "prometheus"
version = "0.1.0"
description = "Prometheus desktop shell"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
getrandom = "0.3"
serde_json = "1"
tauri = { version = "2.11.6", features = [] }
