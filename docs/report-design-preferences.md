# 报告设计偏好

## Brief：短标签与长说明表格

2026-09-19，用户明确回复“批准采纳”，采纳桌面表格短标签完整、说明列边界清楚的方案。适用范围为 Brief 同类表格，不推广到 Standard 或所有表格。

- 先为短标签留足空间，再分配说明列；不靠缩字号或删去必要内容解决拥挤。
- 四列为四字以内标签、两字以内标签、两列说明时，可按需采用16% / 10% / 38% / 36%的候选比例及说明列细分隔线。
- 取舍：长说明可能增加行数；窄屏局部横向滚动。其他结构按内容选择，不固定套用比例。

生成入口：[Brief 模式规则](../src/video_report_agent/skills/video-report/modes/brief.md)中的“表格列宽”，及其链接的唯一 Brief 模板、合成表格示例。评审入口为工作区 report-design-explore Skill，其每轮读取本文。

证据边界：原报告桌面问题已经浏览器观察；用户已采纳候选，但本地小样被浏览器安全策略阻止，候选桌面、窄屏与交互视觉验证尚未完成。此条为有适用条件的已采纳选择，不宣称已跨内容验证。

## 设计探索与选择流程（所有模式）

2026-09-19，用户明确要求：局部设计必须放入原模式模板的实际阅读场景，与既有视觉组件协调。不要另起展示页风格后孤立比较组件。默认保留原纸面、外围背景、字体、字号与标题层级，使用同场景原组件验证搭配效果。

用户看完多个方案并明确选定其中一个（例如回复“D”），即授权采纳并落实到对应模式的生成 Skill、模板和偏好记录；不再只切换预览、强调仍是小样或重复确认。仅明确限制为预览、保留样例或暂不执行时不落地；对象或模式确实不明时才询问。此流程不自动授权提交、推送、部署或重生成历史报告。

## Standard：浅蓝表头与白底正文行

2026-09-19，用户在原 Standard 场景中选定 D 并要求实施。

- 保留浅蓝表头，正文所有行统一纯白，取消蓝白交替条纹。
- 使用浅色细圆角外框与细横线；语义行标题列使用细竖线与说明列分开。
- 复用 Standard 的蓝色、纯白纸面及原有字号体系，保持与浅蓝解释框、浅黄归纳框及白色卡片协调。
- 列宽仍按内容分配；桌面验证发现长说明会挤断四字标签，因此仅 `.table-details` 的语义行标题保留 7.5em 最小宽度，不将样例的固定列宽推广到所有表格。
- 适用范围：Standard 表格，不修改 Brief。取舍：相比交替底色，分组更多依靠细线与留白。

生成入口：[Standard 规则](../src/video_report_agent/skills/video-report/modes/standard.md)与[唯一 Standard 模板](../src/video_report_agent/skills/video-report/assets/report-template.html)。已在原模板组件上下文中检查桌面效果；不代表已重新生成或审查所有历史报告。

## Standard：左侧页外章节导航

2026-09-20，用户在 `balanced-section-nav` 四方向中明确选择 D，并指定侧栏放在纸面左侧；长图不截导航，以便内容开门见山并保持正常长图宽度。

- 只在章节较多、确有定位价值的 Standard 长文使用；短报告不生成目录。
- 宽桌面把章节链接固定在白色纸面左侧页外，不占正文纸面和题头后的垂直空间。
- 导航保持轻量白底、细小序号与短标签；不使用长横线、逐项短下划线或自然换行的 6+5 布局。
- 窄于可同时容纳纸面与侧栏的视口时直接隐藏导航，不把 11 项重新塞回正文，也不新增目录按钮；章节标题继续提供阅读定位。
- 报告长图使用 920px 桌面截图宽度，命中同一隐藏规则，因此长图只保留正常纸面内容，不需要另一套截图模板。
- 适用范围仅为 Standard，不修改 Brief。影响后续生成，不重写历史报告。

验证：实际 Standard 模板在 1280×720 浏览器中显示左侧导航，与 860px 纸面间距 13px且无横向溢出；920×900 时导航隐藏、纸面保持 860px、第一章正常接续。长图渲染测试确认 920px `report.png` 隐藏导航。相关模板、长图和模式测试 24 项通过。


## Standard：书签卡片章节导航（替代前述轻量目录外观）

2026-09-21，用户选择 `nav-five` 的 D，并要求去掉底部“正在阅读”和进度条。

- 采用独立浅白书签卡片，章节标签 15px、序号 12px；当前章节使用暖橙左边框、轻微阴影及向纸面伸出 8px 的标识。
- 不显示“正在阅读”、进度条或底部状态区；滚动与锚点跳转更新当前章节。
- 保留原 860px 正文纸面，侧栏在左侧页外；1195–1448px 桌面将纸面右移以容纳加宽目录，1194px 及以下隐藏导航。打印与 920px 长图继续隐藏。
- 仅适用于后续 Standard 生成；不重生成历史报告，不影响 Brief。

2026-09-21 补充：导航标题固定为“目录”，不显示章节数量；点击导航平滑滚动，保留系统减少动态效果偏好。
