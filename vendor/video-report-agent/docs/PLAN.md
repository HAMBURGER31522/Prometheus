# Core 当前计划与待解决问题

> 2026-09-24，基于 `428f4fe`。本轮已完成文档接入；没有从历史候选建议推导新的实施授权或排期。

## 已明确的持续工作

后续按 [AGENTS.md](../AGENTS.md) 维护项目状态、架构和重要决策。设计与模板变更沿用已有模式范围、真实内容对照和证据口径，见 [设计偏好](report-design-preferences.md)。具体功能没有新增已确认排期。

## 已知问题与验证缺口

- `usage.py::_asr_costs` 仅显式将 `transcript_reused_from` 计为复用零费用，尚未覆盖独立 `asr_reused_from`。这是现有估算边界，修复未纳入本轮。
- Brief 表格采纳记录中的候选桌面、窄屏与交互视觉验证仍未完成；不能因规则已采纳而关闭该缺口。
- Standard 通用图示规则已写入，弱模型真实生成的质量提升尚无本轮验证。若继续评估，按既有同源、固定配置口径比较，不以静态规则检查替代。

以上是现有材料支持的未闭环事项，不代表新增实施承诺。真实材料与结果保存在忽略的 `.local/evals/`；公开 smoke 不充当真实质量基准。
